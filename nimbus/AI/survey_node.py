# use: Loops YOLO, Depth node, rtabmap node
# inputs: intent = "survey", video, depth cam, camera pose
# output: scene objects (object pose array of objects and their 3d position)

def survey_node():
    print("microwave noises")